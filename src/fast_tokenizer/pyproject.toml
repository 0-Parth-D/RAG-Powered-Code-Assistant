[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "fast-tokenizer"
version = "0.1.0"
description = "A fast C++ tokenizer for hybrid RAG search."
dependencies = []